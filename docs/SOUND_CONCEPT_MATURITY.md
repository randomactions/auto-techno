# Sound Concept Maturity

This register separates Auto Techno's durable musical language from the current
DSP used to express it. The semantic owner, continuation, correction, and evidence
contract should survive a renderer rewrite; numeric coefficients and local DSP
topology may change when a later engine revision can prove a better consequence.

The detailed observation and source trail lives in
[`history/TASTE_EXPERIMENTS.md`](history/TASTE_EXPERIMENTS.md). Current product
and qualification policy remain authoritative in [`PRODUCT.md`](PRODUCT.md) and
[`SOUND_QUALITY.md`](SOUND_QUALITY.md). This file is an engineering map, not a
claim that the present sound is mature or professionally qualified.

## Maturity contract

Every sound concept has four separately reviewable parts:

1. **Durable intention** — the musical relationship the director and resolved
   score own.
2. **Current realization** — the versioned DSP that presently changes PCM.
3. **Truth boundary** — same-pass evidence, continuation, bounds, and exact
   evaluator-owned home correction.
4. **Maturation path** — richer DSP that may replace the realization after a
   measured deficit and a versioned qualification pass.

A mature rewrite must not silently reinterpret the intention. It must keep one
canonical director/score/renderer path, remain deterministic and off the
real-time callback, preserve protected roles and exact neutral behavior, replace
rather than duplicate the old DSP, emit truthful consequence evidence, advance
the affected engine/schema identities, and pass the current qualification and
runtime-safety gates.

## Current concept register

| Durable intention | Current realization | Truth boundary | Later serious DSP direction |
| --- | --- | --- | --- |
| Weak-position percussion should reveal groove without becoming a second clock. | One bounded weak-sixteenth vocabulary, physical strike/decay articulation, and 3-3-2 accent/ghost grouping on existing onsets. | Event-local score-to-dry-PCM hashes, physical metrics, bounded density, and one canonical carrier. | Better drum physical models, oversampled nonlinear transients, role-aware excitation, and perceptually calibrated attack/body/tail relationships. |
| A tuned foundation accent should have material identity and modal causality rather than behave as a root-only synthetic tom. | Up to two existing tuned-percussive foundation events resolve score-owned modal degrees and bounded excitation, damping, brightness, inharmonicity, and intensity into one deterministic six-mode resonator with four fixed continuation slots. | Exact score/event binding, requested and measured pitch, attack/body/tail ratios, centroid, masking, pole stability, protected/full pass equality, dry-PCM hash, and rate-normalized 44.1/48 kHz qualification. | Richer physical resonators, coupled bodies, strike-position models, and calibrated material families may replace this six-mode realization in place. Preserve score-owned modal pitch, fixed bounded continuation, protected routing, exact consequence evidence, and the single primary evaluator; never add a parallel drum engine or preset selector. |
| Ordinary upper percussion should breathe around an open-hat companion without changing the groove. | A post-arbitration semantic closed-hat decay role shortens only the matching existing tail. | Exact event pairing, dry hash and envelope evidence; neutral decay is bit-identical. | Continuous or material-aware decay models, richer cymbal resonators, and calibrated cross-role masking while preserving onset and role ownership. |
| Existing upper-percussion tails should leave room when another role owns the foreground, while retaining their natural body when percussion owns the focus, the score asks for an intentional pileup, or the phrase returns to identity. | A post-arbitration `UpperPercussionTailRole` reuses the existing clap, open-hat, and metallic voices. `foregroundClearance` preserves the first 8 ms and applies one bounded raised-cosine release to a final multiplier of 0.25 after each voice's canonical sample generation; `naturalBody` is bit-identical. | Exact score/event pairing, bounded frame geometry, attack/full/tail hashes and RMS, tail-to-attack and difference evidence, protected/full pass equality, complete per-bar candidate retention, and calibrated primary-profile bounds. No new track, instrument, effect return, continuation buffer, or callback work is introduced. | Replace the provisional state-free curve with material-aware MSEGs, coupled cymbal/body models, envelope-aware multiband or dynamic release, and calibrated foreground-masking control when the primary evaluator exposes a repeatable clearance or natural-body deficit. Preserve the score role, exact neutral path, attack window, event ownership, and evidence; replace this realization in place instead of adding a second percussion-tail chain. **Revisit when:** accepted canonical checkpoints repeatedly fail upper-percussion tail clearance or tail/body translation without a score, route, or safety defect. **Supersedes:** the raised-cosine v1 projection and its event-local tail metrics rather than coexisting with another tail controller. |
| Repetition should develop through coordinated upper-role relationships rather than random replacement. | Interlock chapters, a 3-by-5 relational cycle, narrative presence, and bounded role admission reinterpret one stable score identity. | Typed plan identity, role-local upper evidence, bounded continuation, identity-return home state, and evaluator-owned home correction. | More expressive modulation networks, smooth multi-rate control, richer long-memory gestures, and calibrated phrase-coherence evidence without adding another sequencer. |
| A newly started performance should be genuinely fresh while remaining coherent and exactly reproducible after its identity is chosen. | The App draws one opaque `UInt64` before planning, installs it as the sole director/root-score identity, retains it through pause, recovery, correction, and continuation, and rotates it only at a complete session boundary. The secondary New Set action explicitly performs that boundary, clears all prior continuation/cache/feedback identity, and automatically starts only after the fresh first phrase is prepared. Preparation keys bind the seed; no entropy reaches detached rendering or the callback. | Injected fixed-seed plan/PCM equality, distinct-seed plan/PCM divergence, immediate collision avoidance, New Set/session-reset rotation, clean presentation reset, seed-bound cache identity, and existing deterministic architecture/preflight contracts. | Replace the v1 system-entropy draw and scalar seed in place with stronger identity provenance, opt-in replay tokens, calibrated cross-session novelty memory, or evaluator-owned repetition evidence only when collisions, insufficient creative range, or provenance deficits are measured. Preserve explicit-seed reproducibility, future-boundary application, callback silence, one director, one renderer, and one primary evaluator; do not add a random mode, selector, or second performance engine. **Revisit when:** independently started accepted sessions repeatedly collide in plan/PCM identity or fail calibrated novelty evidence without an in-session coherence defect. **Supersedes:** the v1 App seed source and collision step rather than coexisting with another randomness mechanism. |
| Shadow and response attacks may spread and reconverge while the anchor and protected rhythm remain fixed. | A score-owned 16-bar aperture delays shadow by half depth and response by full depth. | Exact score/render/applied-gate fingerprints, role-local hashes, literal endpoint zero, and force-home neutrality. | Fractional-delay scheduling, higher-resolution expressive timing, and perceptual phase-spread limits with the same role policy. |
| A foreground tonal line may feel performed while its electronic rhythmic frame stays exact. | In one naturally eligible melodic lock, the first retriggered anchor stays on-grid and later anchors use deterministic 0.018/0.036-sixteenth delays; no note, pitch, gate duration, velocity, patch, effect, companion, drum, or transport changes. | A typed timing relation, exact anchor-offset pattern fingerprint, actual onset/gate evidence, isolated anchor hash/peak/RMS, protected-role zero offsets, and exact force-home alignment. | Replace rounded-frame micro-delays with fractional-delay scheduling and a higher-resolution score-owned performance curve, then calibrate phrase-level timing/attack legibility across instruments and routes. Do not add free-running jitter or another sequencer. **Revisit when:** the anchor-offset record is complete at establishment/contrast/return checkpoints and the calibrated primary evaluator rejects the aligned realization for insufficient foreground timing/attack differentiation without a masking, safety, or structural regression. **Supersedes:** the v15 rounded-frame two-tier offset and its `upper-timing-anchor-offsets.typed.v1` projection in place; it must not coexist as another performance lane. |
| An existing tonal protagonist may emerge from a veiled introduction without adding another musical lane. | Eligible narrative anchor notes reuse the canonical Resonant Mono or Tonal Motion voice and apply a score-owned `emerging` aperture to that voice's existing low-pass cutoff; home is the exact prior cutoff. | The typed note relation, plan identity, architecture-local event and anchor hashes, actual cutoff extrema, active/eligible binding, force-home correction, and professional cutoff/activity metrics prove one causal score-to-PCM path. | Replace the scalar cutoff aperture in place with a higher-order score-owned spectral trajectory, envelope-aware or multiband reveal, formant-safe morph, or oversampled filter topology only when calibrated foreground legibility, masking, or artifact evidence exposes a repeatable deficit. Preserve the existing protagonist, exact home path, sends, role ownership, and evidence; do not retain the v1 scalar reveal beside another reveal chain. **Revisit when:** accepted canonical checkpoints repeatedly fail foreground arrival or spectral-motion evidence without a harmony, timing, density, route, or safety defect. **Supersedes:** the v1 anchor-cutoff projection and its compact architecture-local evidence. |
| A delayed effect return may gain body without feeding a driven signal back into its own memory. | The existing filtered pulse-echo return receives bounded, state-free nonlinear low-level lift outside feedback. | Pre/post hashes and metrics, exact transition/changed-sample witnesses, timely-onset eligibility, raw tail continuation, and neutral endpoints. | True upward or parallel dynamics, oversampled colour, multiband or envelope-aware body recovery, stereo-safe diffusion, and calibrated effect-to-dry intelligibility. |
| An existing percussion event may answer itself later in a phrase or become the anticipatory energy that makes an already-owned release feel inevitable. | One canonical protected percussion-return chain has two score-owned relations: `gatedEcho` admits the established filtered delay inside a later contrast window, while `anticipationSwell` reverses that same bounded wet tail under a raised-cosine crescendo only on the second kick-withheld bar before recovery. No onset, track, instrument, bus, reusable loop, or cross-bar state is added. | Exact relation and kick-syntax binding, source/output geometry, protected-path hashes and RMS, early/late energy and rise dB, nonzero counts, exact-zero endpoints, pass equality, and the calibrated primary profile keep the reuse causal. Neutral and legacy gated paths retain their prior identity. | Replace the provisional reverse-buffer/crescendo realization in place with a transient-aware reverse convolution, fractional or multitap stereo delay, denser diffusion, spectral shaping, or controlled nonlinear colour only when calibrated buildup, masking, or artifact evidence exposes a repeatable deficit. Preserve the existing percussion owner, release boundary, exact neutral and gated behavior, and causal evidence; do not add a second riser track or return bus. **Revisit when:** accepted release checkpoints repeatedly fail anticipation-rise or translation evidence without a kick-syntax, density, route, or safety defect. **Supersedes:** the v1 reverse-wet projection and its early/late metrics rather than coexisting with another buildup effect. |
| Distant upper material should occupy a diffuse field without weakening the centered rhythm. | One scene-derived eight-line Householder FDN replaces the former single long mono feedback delay; the existing score-owned carrier send and separate early reflection remain authoritative. | Route-normalized delay/decay/damping bounds, exact input and stereo-wet hashes, level/correlation/activity evidence, bounded continuation, score binding, and exact protected kick/foundation identity. | Modulated or multiband decay, higher-order damping, and calibrated spatial/mono-translation targets may replace FDN v1 only inside the same late-field owner; do not add another reverb engine or selector. |
| A major break may fracture its own rhythm into recognisable but transformed material. | One bounded phrase-local resampler reads an exact already-rendered dry-percussion window during Broken Suspension or Ambient Drift, using that bar's resolved dry kick when percussion is intentionally absent, and places three to four score-owned forward/reverse triggers at 0.5x to 2x. A fixed 16-sample-radius Hann-windowed sinc kernel replaces linear lookup in place; faster playback applies a `0.94 / rate` anti-alias cutoff, while unity integer positions remain exact. It captures no loop and owns no cross-bar buffer. | A deterministic 18 kHz/2x probe reduced the false 12 kHz fold-back from amplitude `0.5` to below `0.000079` (about 76 dB), while an in-band transposition retains amplitude and pitch. Source kind, source/output PCM hashes, trigger and reverse counts, rate extrema, finite RMS, protected/full pass equality, eligibility binding, and exact identity-return neutrality keep the canonical use causal. | Replace the v2 fixed-radius kernel in place with a polyphase or higher-stopband converter, transient-aware slice boundaries, stereo-preserving grains, or phrase-spanning source choice only when calibrated alias, click, transient, latency, or repetition evidence identifies another deficit. Preserve app-owned PCM provenance, bounded immutable trigger geometry, unity exactness, and the single renderer; do not retain a second resampler or granular engine. **Revisit when:** accepted major-break checkpoints repeatedly fail alias, boundary, transient, translation, preparation-latency, or source-recurrence evidence without a score, route, or unrelated-family defect. **Supersedes:** the linear interpolation lookup rather than coexisting with it. |
| A sparse tonal motif may become a deliberate electronic cascade without surrendering timing or harmony to a hidden sequencer. | Eligible melodic, acid, or peak characters replace sparse motif anchors with a fully resolved 8- or 16-step modal arpeggio. Direction, rotation, rate, octave, duration, velocity, pitch, and instrument remain immutable score data. Structural markers stay sparse so the established sustained-wash payoff retains ownership; Tone chapters stay sparse so their complementary spectral relation remains causal. | Complete per-bar geometry, direction/rate/octave bounds, exact upper-note ownership, deterministic plan fingerprint, and neutral major-break/marker/Tone paths. | Ratchets, ties, rests, chord-aware register arcs, and calibrated phrase-density control may replace this first complete vocabulary; never add a renderer-side clock or independent arpeggiator state. |
| Atmospheric harmony should be genuinely polyphonic and should move as connected voices rather than unrelated block chords. | A fixed four-voice pad renderer maintains independent oscillator/filter/envelope state per voice. The score evaluates bounded inversions against the last accepted chord, rewards common tones, penalizes leaps, and can favour contrary outer motion; the accepted voicing crosses phrase boundaries as compact score continuation. | Exact four ratios and pad PCM hash/RMS/peak, function and common-tone counts, total movement and maximum leap, typed continuation identity, deterministic replay, and exact neutral ineligible behavior. | Richer band-limited oscillators, per-voice articulation, suspensions and passing tones, longer harmonic syntax, and calibrated tonal/atonal tension evidence may replace the current triadic-extension vocabulary while keeping four explicit voices and one canonical continuation owner. |
| Harmony should become progressively knowable across a phrase, then contract again, without adding another chord track. | The existing four-voice pad and its dependent arpeggiator carry one score-owned `concealed -> partial -> revealed` disclosure stage. Lock phrases expose tonic first and a two-function preview later; Major Break traverses the existing four-function vocabulary; all other eligible contexts retain the established resolver. | Exact local-bar/phrase-length replay, stage/function bounds, four pad ratios and PCM evidence, score/render arpeggiator pitch fingerprints, typed plan identity, calibrated revealed prevalence and distinct-function count, exact identity neutrality, and accepted harmonic continuation prove one causal owner. | Replace the coarse stage projection in place with longer functional syntax, suspensions, passing tones, tension-aware inversions, or a shared multi-role harmonic frame only after calibrated predictability, tonal-tension, masking, or repetition evidence exposes a repeatable deficit. Preserve the pad owner, deterministic disclosure arc, four explicit voices, accepted continuation, exact neutral path, and one primary evaluator. **Revisit when:** accepted canonical checkpoints repeatedly fail harmonic predictability, payoff, or repetition evidence without a density, timbre, route, or safety defect. **Supersedes:** the v1 discrete stage/function projection and compact pitch binding; do not add a second progression, pad lane, or harmonic engine. |
| A held atmospheric chord may recover rhythmic energy without becoming another note pattern or effect lane. | Naturally resolved latter-half major-break pads reuse their existing low-pass and spatial-reverb send under one score-owned three-sixteenth relation whose phase follows absolute bar time. The same relation closes, opens, and closes both dry pad and its existing send with route-derived 6 ms raised-cosine edges while synthesis and spatial continuation advance. Early, minimalized, structural-marker, identity, and ineligible paths are exact neutral. | Exact relation, phase and 16-step pattern fingerprint, applied filter/send/gate extrema, route-derived transition frames, open/closed counts, pre/post dry and send hashes, exact closed-step silence, same-pass difference RMS, prepared binding, and calibrated activity plus level-relative filter/spatial/amplitude consequence metrics prove one causal path. | Replace the v1 discrete scale and amplitude-mask projection in place with higher-resolution control-rate envelopes, tempo-aware variable duty, transient-conditioned edges, higher-order or nonlinear filtering, multiband or stereo-linked gating, or denser diffusion only when calibrated motion, masking, transition, or translation evidence exposes a repeatable deficit. Preserve the existing pad and three-step score owner, absolute-time phase, advancing continuation, exact neutral behavior, and causal evidence; do not add a second sequencer, pad track, trigger lane, sidechain bus, or generic gate effect. **Revisit when:** accepted major-break checkpoints repeatedly fail rhythmic-motion or translation evidence without a harmony, density, route, or safety defect. **Supersedes:** the v1 three-step cutoff/send/amplitude projection and its difference metrics. |
| A short foundation voice may imply a deeper cross-bar groove while the kick retains the physical clock. | Eligible four-bar-aligned Lock pairs replace only their existing bass events with complementary two-bar masks `0x8248` and `0x4824`, yielding one continuous three-sixteenth relation and a phrase-boundary reset. The established protected foundation route, Bass Pluck patch, score swing, TPT filter, ADAA core, and dry center placement remain the only sound path. | Exact relation, pair phase, score/render event counts and masks, applied start-frame fingerprint, dry foundation hash/peak/RMS, Bass Pluck assignment, protected/full pass equality, canonical preflight replay, calibrated active prevalence and crest evidence, and exact established fallback prove one causal path. | Replace the integer-grid v1 projection in place with sub-sample scheduling, tempo-aware articulation, richer score-owned pitch/envelope trajectories, improved Bass Pluck synthesis, or filtered spatial reinforcement only when calibrated timing, masking, translation, repetition, or artifact evidence exposes a repeatable deficit. Preserve the protected foundation owner, two-bar reset, exact neutral behavior, and causal evidence; do not add a second bass sequencer, track, or effect chain. **Revisit when:** accepted Lock checkpoints repeatedly fail groove-legibility or low-end-translation evidence without a kick, harmony, route, or safety defect. **Supersedes:** the v1 two-mask integer-onset projection and its compact foundation-rhythm evidence. |
| A pre-kick foundation absence should clarify an already-owned kick without thinning the whole mix or introducing another control loop. | The eligible dotted Bass Pluck one step before kick 4 or 12 receives one score-derived state-free raised-cosine terminal release from `kick - 0.1875 step` to exact zero at `kick - 0.0625 step`; all other events and bars remain literal neutral on the same protected path. | Exact score-event and bass/kick binding, natural-crossing proof, route-derived frame geometry, positive release/silence counts, same-pass silence hash/peak/RMS, full/protected equality, candidate tamper rejection, a near-zero primary metric, and one non-compensable contamination attack prove the pocket rather than infer it from the mix. | Replace the two-point curve in place with envelope-phase-aware note shortening, transient-conditioned ducking, fractional-step articulation, or a richer foundation model only when calibrated masking, low-end hierarchy, or transient evidence exposes a repeatable deficit. Preserve the dotted score owner, exact neutral path, protected routing, deterministic continuation, and one primary evaluator; do not add a sidechain bus, global filter, parallel bass path, or user control. **Revisit when:** accepted dotted checkpoints repeatedly fail low-end hierarchy or masking evidence without a score, route, or safety defect. **Supersedes:** the v1 terminal curve and exact-zero proxy rather than coexisting with another ducking mechanism. |
| Temporarily removing an established kick can make the same weak-pulse context metrically ambiguous; restoring it should reframe the groove. | A paid-debt energy-release arc owns `grounded -> withheld -> withheld -> recovery` and restores the unchanged step-zero kick. | Exact kick score/render masks and hashes, zero withheld stems, surviving weak-pulse/motif evidence, positive recovery, and grounded setup. | Perceptually calibrated ambiguity/recovery evidence, richer but still score-owned anchor syntax, and translation-aware low-end recovery without moving transport or bar lines. |
| A climax can gain weight from a brief authored absence between its rise and its already-earned recovery, without requiring a new sound source. | On only the final kick-withheld energy-release bar at macro position 14, the existing weak-pulse and anticipation owners end at step 12, one state-free 8 ms raised-cosine projection closes the full mix, and steps 12--16 remain exactly silent while all canonical DSP continuation advances underneath. The next step-zero recovery remains unchanged. | Exact score relation, macro position, `[3, 7, 11]` pre-hang pulses, route-derived release/silence geometry, pre/post/silence hashes, exact-zero peak/RMS/nonzero count, post-hang/pre-live-master equality, neutral identity, multirate proof, a calibrated silence metric, and one non-compensable contamination attack. No track, instrument, effect chain, new event, callback work, or persistent state is added. | Replace the v1 output projection in place with role-aware decay choreography, transient-conditioned muting, tail-safe spectral evacuation, or a higher-resolution tension/recovery envelope only when calibrated transition, boundary, or payoff evidence exposes a repeatable deficit. Preserve the paid-debt kick owner, exact terminal absence, advancing continuation, unchanged recovery, deterministic score/evidence, and one primary evaluator; do not add another break sequencer, silence bus, riser, impact, or alternate climax mode. **Revisit when:** accepted release checkpoints repeatedly fail payoff, boundary, or translation evidence without a kick-syntax, route, or safety defect. **Supersedes:** the v1 8 ms full-mix release and exact-zero proxy rather than coexisting with another hang mechanism. |
| The complete kick source should retain an audible body without one narrow spike consuming the later mix's headroom. | Every existing kick event passes its final body + sub + click sum through one fixed first-order ADAA tanh conditioner before the canonical detector and audible buses. No score event, track, instrument, send, master stage, or user control is added. | Event-local typed pre/post hashes, peak/RMS/crest, physical attack/body RMS, upper-mid energy, score/render counts and masks, exact withheld silence, detector/audible scaling, protected/full equality, and four calibrated source-dynamics dimensions keep the source correction causal. | Replace the v1 fixed curve in place with oversampling or higher-order ADAA, transient-aware upward/parallel dynamics, a multiband source contour, or richer physical kick synthesis only when calibrated crest, attack/body, upper-mid translation, alias, or master-work evidence exposes a repeatable deficit. Preserve kick score geometry, detector/audible ownership, protected center routing, exact withheld silence, and the single primary evaluator. **Revisit when:** accepted canonical checkpoints repeatedly fail kick crest, attack/body, spectral-presence, or master-headroom evidence without a score, route, or terminal-safety defect. **Supersedes:** `kick-source-dynamics.adaa-tanh.v1` and its compact pre/post evidence; do not retain it beside another kick track or dynamics chain. |
| A climax should pay an earlier dramatic obligation rather than appear as an unrelated loud section. | The existing director opens bounded contrast or major-break debt, then an energy release resolves it; when the existing kick-syntax relation is present, that payoff also owns the grounded setup, two-bar kick withholding, and structural recovery. Source 20 adds no new PCM; it makes that long-form cause explicit. | Candidate schema 16 fingerprints the exact paid debt records, retains contrast/major-break counts and temporal bounds, cross-checks the optional four-bar kick consequence, and keeps debt-free phrases inactive. | Replace the current coarse phrase-kind/debt and optional fixed four-bar recovery realization with a calibrated multi-stage energy trajectory, higher-resolution transition envelopes, and role-aware payoff orchestration only after long-form evidence exposes a repeatable deficit. Preserve debt ownership, exact future-boundary application, home correction, and causality; do not add a second break sequencer or off-grid transport. **Revisit when:** the calibrated primary evaluator rejects a structurally valid debt-to-release phrase for deficient tension/payoff trajectory without safety or identity regression. **Supersedes:** the v16 compact climax-arc projection and current kick-syntax realization in place rather than coexisting with another climax mode. |
| Resonant Mono should distinguish ordered hollow colour from metallic tension as one patch family. | Acid Thread and Acid Sequence use bounded two-operator phase-modulation relations with an in-note dark-bright-dark aperture; all Resonant Mono patches now pass through one shared TPT SVF with first-order ADAA tanh shaping. | Exact relation/event fingerprints, operator-tap hashes and metrics, core input/output hashes, applied cutoff/Q/drive/band-mix ranges, rate bounds, continuation, and zero operator path for non-acid and protected foundation. | Oversampling or higher-order antialiasing, more operators or feedback, richer envelopes, stereo-safe upper harmonics, and calibrated low-band/crest/alias bounds may replace core v1 in place; do not restore local filters or add another renderer. |
| A rising close cluster should build harmonic tension as one transition object without adding melody or density. | The existing Metal Veil transition interprets its existing three oscillator phases as adjacent semitone components while following the already resolved upward trajectory. | Transition-only assignment semantics, exact component ratios and upward frequency facts, an isolated dry cluster hash/metrics, unchanged non-transition texture, and exact home-correction behavior. | Oversampled or band-limited oscillator banks, microtonal/spectral spreading, physical string or resonator models, controlled divergence/reconvergence, and perceptually calibrated tension/harshness evidence. |
| A sparse response can feel electrically animated by revealing the dense upper partials of a low periodic source without adding a bass lane or broadband-noise layer. | Broken Suspension assigns the response-only Voltage Arc patch inside the existing Spectral Texture architecture. A bounded low folded polyBLEP saw receives prefilter colour, a moving resonant TPT band-pass isolates its upper tail, and the existing filtered-reverb send retains the response in space. | Exact patch/use/relation ownership, folded-source and center bounds, free-running LFO continuation, isolated dry hash/peak/RMS/crest, low-band suppression, positive upper-band energy, score-to-event binding, candidate completeness, one higher-only-safer professional dimension, and a non-compensable disconnected-tail attack. | Replace the v1 oscillator/filter projection in place with oversampled oscillator banks, higher-order antialiasing, nonlinear filter models, envelope-followed motion, multiband diffusion, or calibrated stereo decorrelation only when the primary evaluator exposes a repeatable motion, harshness, masking, translation, or narrative deficit. Preserve the response-owned patch and relation, exact score/continuation/evidence contract, and one renderer; do not retain a parallel zap effect. **Revisit when:** accepted Broken Suspension responses repeatedly fail upper-tail energy, masking, boundary, or rate-translation evidence without a score, route, or safety defect. **Supersedes:** the v1 polyBLEP/TPT/LFO realization rather than coexisting with another track, instrument, or effect chain. |
| One familiar short tonal gesture may occupy a release boundary at a larger temporal scale without becoming another layer. | The last eligible Tonal Motion anchor at the canonical energy-release marker raises the same note's bounded sustain target and release time through `sustainedWash`; no onset, pitch, gate, instrument, effect route, or density changes. | The typed note relation and attempt eligibility bind an exact active event, base/applied sustain and release, an isolated dry hash, attack/tail metrics, continuation state, and exact force-home neutrality. | Replace the provisional ADSR projection with a higher-resolution MSEG or exponential envelope, envelope-aware dynamics, oversampled tail colour, or controlled diffusion only after calibrated tail/body and masking evidence identifies a deficit. |
| Timbre and effects can speak as a sentence—call, delayed response, and turnaround—while drums retain attention priority. | The existing motif/response/transition roles, narrative contour, unsynced and pulse echo, canonical FDN late field, gated percussion return, and structural gestures already compose this relationship; Source 16 adds no parallel effect-phrase engine. | Current role, effect-state, pulse-return, spatial-FDN, gated-return, masking, stem, protected-rhythm, transaction, and continuation evidence remain the owners. | Add phrase-level effect-body and call/response/turnaround evidence first; only then consider envelope-aware upward dynamics, denser diffusion, spectral morphing, or shared-return orchestration. Preserve the drums' attention budget and consolidate the existing paths rather than layering another phrase system. |

## Revisit triggers

A concept is ready for another DSP level only when at least one exact canonical
checkpoint exposes a repeatable deficit that existing evidence cannot resolve,
and the proposed replacement can be evaluated without subjective constants from
the motivating video. Typical triggers include aliasing or artifact evidence,
weak role attribution, poor translation across supported rates, implausible
envelope geometry, insufficient phrase consequence, or a calibrated relational
profile failure.

Human listening, videos, and comments may identify the hypothesis. They do not
select an implementation or promote it. The replacement must still pass
deterministic automated qualification, exact-head CI, app/route checks, and the
separate physical-output soak required for its release claim.
